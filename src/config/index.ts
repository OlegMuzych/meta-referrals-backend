import appConfig from './app.config';
import databaseConfig from './database.config';
import usermanagerConfig from './usermanager.config';

export const configLoads = [databaseConfig, appConfig, usermanagerConfig];
